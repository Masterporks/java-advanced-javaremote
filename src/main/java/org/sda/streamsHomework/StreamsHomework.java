package org.sda.streamsHomework;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

public class StreamsHomework {
    public static void main(String[] args) {



    List<String> removeChar = Arrays.asList("John", "Sarah", "Mark", "Tyla", "Ellisha", "Eamonn");
    // removeChar.stream().map(name ->name.substring(1,name.length()-1)).sorted().forEach(name ->sout (name+ ""))

}
}
